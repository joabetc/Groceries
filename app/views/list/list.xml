<Page loaded="loaded">
    <GridLayout>
        <ListView items="{{ groceryList }}">
            <ListView.itemTemplate>
                <Label text="{{ name }}" horizontalAlignament="left" verticalAlignment="center"/>
            </ListView.itemTemplate>
        </ListView>
    </GridLayout>
</Page>